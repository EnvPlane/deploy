name: Publish control-plane Helm chart

on:
  push:
    tags:
      - "control-plane-chart-v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: azure/setup-helm@v4
      - id: chart
        name: Resolve chart version
        run: echo "version=$(awk '/^version:/{print $2; exit}' deploy/helm/envpilot-control-plane/Chart.yaml)" >> "$GITHUB_OUTPUT"
      - name: Verify canonical Helm chart sources
        run: ./scripts/check-canonical-chart-sources.sh --deploy "$GITHUB_WORKSPACE"
      - name: Package chart
        run: |
          helm dependency build --skip-refresh deploy/helm/envpilot-control-plane
          helm package deploy/helm/envpilot-control-plane --destination dist
      - name: Authenticate to GHCR
        run: echo "$GHCR_TOKEN" | helm registry login ghcr.io --username "$GITHUB_ACTOR" --password-stdin
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN || secrets.GITHUB_TOKEN }}
      - name: Reject an existing OCI version
        run: ./scripts/check-oci-chart-version-available.sh --repository oci://ghcr.io/envpilot/envpilot-control-plane --version '${{ steps.chart.outputs.version }}'
      - name: Publish OCI chart
        id: push
        run: |
          set -euo pipefail
          output="$(helm push dist/envpilot-control-plane-*.tgz oci://ghcr.io/${{ github.repository_owner }} 2>&1 | tee /dev/stderr)"
          digest="$(printf '%s\n' "$output" | awk '/^Digest:/{print $2; exit}')"
          [[ -n "$digest" ]]
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
      - id: dispatch_auth
        name: Select deploy dispatch authentication
        env:
          CLIENT_ID: ${{ secrets.ENVPILOT_AUTOMATION_APP_CLIENT_ID }}
          APP_KEY: ${{ secrets.ENVPILOT_AUTOMATION_APP_PRIVATE_KEY }}
          PAT: ${{ secrets.ENVPILOT_AUTOMATION_PAT }}
        run: |
          if [[ -n "$CLIENT_ID" && -n "$APP_KEY" ]]; then echo method=app >> "$GITHUB_OUTPUT"; elif [[ -n "$PAT" ]]; then echo method=pat >> "$GITHUB_OUTPUT"; else exit 1; fi
      - id: dispatch_app
        if: steps.dispatch_auth.outputs.method == 'app'
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.ENVPILOT_AUTOMATION_APP_CLIENT_ID }}
          private-key: ${{ secrets.ENVPILOT_AUTOMATION_APP_PRIVATE_KEY }}
          owner: envpilot
          repositories: deploy
          permission-contents: write
      - name: Dispatch chart publication
        if: steps.dispatch_auth.outputs.method == 'app' || steps.dispatch_auth.outputs.method == 'pat'
        env:
          GH_TOKEN: ${{ steps.dispatch_app.outputs.token || secrets.ENVPILOT_AUTOMATION_PAT }}
        run: ./scripts/dispatch-chart-published.sh --component control-plane --chart envpilot-control-plane --version '${{ steps.chart.outputs.version }}' --digest '${{ steps.push.outputs.digest }}'
