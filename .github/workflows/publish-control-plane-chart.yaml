name: Publish control-plane Helm chart

on:
  push:
    tags:
      - "control-plane-chart-v*"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: azure/setup-helm@v4
      - id: chart
        name: Resolve chart version
        run: echo "version=$(awk '/^version:/{print $2; exit}' deploy/helm/envplane-control-plane/Chart.yaml)" >> "$GITHUB_OUTPUT"
      - name: Verify canonical Helm chart sources
        run: ./scripts/check-canonical-chart-sources.sh --deploy "$GITHUB_WORKSPACE"
      - name: Build chart dependencies
        run: helm dependency build --skip-refresh deploy/helm/envplane-control-plane
      - name: Verify remote-cluster credential RBAC contract
        run: ./scripts/tests/remote-cluster-credential-contract.sh
      - name: Package chart
        run: |
          helm dependency build --skip-refresh deploy/helm/envplane-control-plane
          helm package deploy/helm/envplane-control-plane --destination dist
      - name: Authenticate to GHCR
        run: echo "$GHCR_TOKEN" | helm registry login ghcr.io --username "$GITHUB_ACTOR" --password-stdin
        env:
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN || secrets.GITHUB_TOKEN }}
      - name: Reject an existing OCI version
        run: ./scripts/check-oci-chart-version-available.sh --repository oci://ghcr.io/EnvPlane/envplane-control-plane --version '${{ steps.chart.outputs.version }}'
      - name: Publish OCI chart
        id: push
        run: |
          set -euo pipefail
          output="$(helm push dist/envplane-control-plane-*.tgz oci://ghcr.io/EnvPlane 2>&1 | tee /dev/stderr)"
          digest="$(printf '%s\n' "$output" | awk '/^Digest:/{print $2; exit}')"
          [[ -n "$digest" ]]
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
      - id: dispatch_auth
        name: Select deploy dispatch authentication
        env:
          CLIENT_ID: ${{ secrets.ENVPLANE_AUTOMATION_APP_CLIENT_ID }}
          APP_KEY: ${{ secrets.ENVPLANE_AUTOMATION_APP_PRIVATE_KEY }}
          PAT: ${{ secrets.ENVPLANE_AUTOMATION_PAT }}
        run: |
          if [[ -n "$CLIENT_ID" && -n "$APP_KEY" ]]; then echo method=app >> "$GITHUB_OUTPUT"; elif [[ -n "$PAT" ]]; then echo method=pat >> "$GITHUB_OUTPUT"; else exit 1; fi
      - id: dispatch_app
        if: steps.dispatch_auth.outputs.method == 'app'
        run: ./scripts/obtain-github-app-token.sh
        env:
          ENVPLANE_AUTOMATION_APP_CLIENT_ID: ${{ secrets.ENVPLANE_AUTOMATION_APP_CLIENT_ID }}
          ENVPLANE_AUTOMATION_APP_PRIVATE_KEY: ${{ secrets.ENVPLANE_AUTOMATION_APP_PRIVATE_KEY }}
          GH_APP_OWNER: EnvPlane
          GH_APP_REPOSITORY: deploy
          GH_APP_TOKEN_PERMISSIONS: '{"contents":"write"}'
      - name: Dispatch chart publication
        if: steps.dispatch_auth.outputs.method == 'app' || steps.dispatch_auth.outputs.method == 'pat'
        env:
          GH_TOKEN: ${{ steps.dispatch_app.outputs.token || secrets.ENVPLANE_AUTOMATION_PAT }}
        run: ./scripts/dispatch-chart-published.sh --component control-plane --chart envplane-control-plane --version '${{ steps.chart.outputs.version }}' --digest '${{ steps.push.outputs.digest }}'
