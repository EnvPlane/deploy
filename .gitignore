# Local env
.env
.env.*

# Build/test artifacts
.cache/
bin/
*.test
*.out
coverage.*
*.coverprofile
profile.cov

# Packaged releases
*.tgz
# The canonical umbrella deliberately vendors its four direct child charts so a
# checked-out release can render/package without reaching an external registry.
!deploy/helm/envplane/charts/
!deploy/helm/envplane/charts/*.tgz

# OS/editor
.DS_Store
.idea/
.vscode/
