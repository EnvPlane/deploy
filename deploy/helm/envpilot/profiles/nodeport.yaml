# Direct node access without an ingress or Gateway provider.
access:
  mode: disabled
envpilot-control-plane:
  service:
    type: NodePort
    port: 8080
    nodePort: 30081
envpilot-frontend:
  service:
    type: NodePort
    port: 3000
    nodePort: 30080
