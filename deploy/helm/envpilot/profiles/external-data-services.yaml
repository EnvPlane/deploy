# Existing Secret values must contain complete connection URLs. No databases are installed.
envpilot-control-plane:
  postgres:
    mode: external
    external:
      existingSecret: envpilot-postgres-url
      urlKey: database-url
  redis:
    mode: external
    external:
      existingSecret: envpilot-redis-url
      urlKey: redis-url
