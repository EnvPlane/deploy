# Requests provider-managed Kubernetes Services of type LoadBalancer.
access:
  mode: disabled
envpilot-control-plane:
  service: { type: LoadBalancer, port: 8080 }
envpilot-frontend:
  service: { type: LoadBalancer, port: 3000 }
