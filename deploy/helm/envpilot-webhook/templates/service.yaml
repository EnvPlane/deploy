apiVersion: v1
kind: Service
metadata:
  name: {{ include "envpilot-webhook.fullname" . }}
  labels:
    {{- include "envpilot-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "envpilot-webhook.selectorLabels" . | nindent 4 }}
