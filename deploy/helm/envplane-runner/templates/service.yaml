apiVersion: v1
kind: Service
metadata:
  name: {{ include "envplane-runner.fullname" . }}
  labels:
    {{- include "envplane-runner.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "envplane-runner.selectorLabels" . | nindent 4 }}
  ports:
    - name: health
      port: 8080
      targetPort: 8080
      protocol: TCP
