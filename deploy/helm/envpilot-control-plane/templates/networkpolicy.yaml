{{- if and (default true .Values.networkPolicy.enabled) (eq (default (ternary "internal" "disabled" .Values.postgres.enabled) .Values.postgres.mode) "internal") }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "envpilot-control-plane.fullname" . }}-postgres
  labels:
    {{- include "envpilot-control-plane.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "envpilot-control-plane.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: postgres
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector:
            matchLabels:
              {{- include "envpilot-control-plane.selectorLabels" . | nindent 14 }}
      ports:
        - protocol: TCP
          port: {{ .Values.postgres.service.port }}
{{- end }}
