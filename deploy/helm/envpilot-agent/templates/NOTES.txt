EnvPilot cluster agent has been installed.

Release:
  Name: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

Control plane:
  URL: {{ default "not configured" .Values.controlPlane.url }}

The agent uses a read-only ClusterRole to observe EnvPilot namespaces, FluxCD Kustomizations,
HelmReleases, workload readiness, ingress state, and Kubernetes events.
